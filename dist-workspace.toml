[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.33.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["x86_64-unknown-linux-gnu"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# `release.yml` is generated but customized after generation (tag trigger
# limited to the `v<version>` application tag plus a `workflow_dispatch`
# trigger, because the tag is created with the default GITHUB_TOKEN and
# therefore does not trigger workflows). Re-apply those edits with
# `python3 scripts/patch_release_workflow.py` after regenerating, and keep
# `allow-dirty` so cargo-dist accepts the difference from its template.
allow-dirty = ["ci"]
# The Release-plz workflow creates the single GitHub Release `v<version>` as a
# *draft* (release-plz.toml runs with `git_release_enable = false`, so it never
# creates a release per crate). With `create-release = false` cargo-dist assumes
# that draft exists, uploads the shell installer / archive / checksums to it and
# publishes it (`gh release edit --draft=false`). `build.yml` then attaches the
# image + Helm chart tarballs to the same release.
create-release = false

# Whether to install an updater program
install-updater = false
